import { SignupForm } from "@/components/AuthForm";

const SignupPage = () => {
  return (
    <main className="h-dvh w-full flex items-center justify-center bg-gray-900">
      <SignupForm />
    </main>
  );
};

export default SignupPage;
